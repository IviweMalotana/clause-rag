# Web (Next.js) environment.
# Local dev points at the local FastAPI server. On Vercel, set this to your
# deployed Railway API URL (e.g. https://clause-api.up.railway.app).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
